import styled from '@emotion/styled';
import { Grid } from '@mui/material';

export const StatsPieChartStyled = styled(Grid)`
  pointer-events: none;
  touch-action: none;
`;

export const StatsPieChartLegendStyled = styled.div`
  height: 150px;
`;
